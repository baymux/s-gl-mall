spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://111.231.89.200:3310/mall_wms?useUnicode=true&characterEncoding=utf-8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss



mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

logging:
  level:
    com.bay.mall.ware: debug